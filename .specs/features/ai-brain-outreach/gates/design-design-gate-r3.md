# Fase 2 — design-gate (design) — rodada 3

**Veredito: PASS** (0 BLOCKER, 4 WARN).

- W1 aceito: Extrator sem contrato com o Q7 (dívida já registrada).
- W2 → Rafael: `manage-instance` fora de `set_webhook`/`get_webhooks` sem auth (pré-existente).
- W3 incorporado na Fase 3: estado de falha de rede/RPC do import CSV vira critério da task de `Prospeccao.tsx`.
- W4 aceito: 3ª cópia do nav (Out of Scope da spec).
